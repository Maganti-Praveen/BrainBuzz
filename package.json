{
  "name": "quiz-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/server.js",   // Start backend server
    "build": "cd frontend && npm install && npm run build",  // Build frontend
    "install": "cd frontend && npm install && cd ../backend && npm install"  // Install dependencies for both frontend and backend
  },
  "dependencies": {
    "cors": "^2.8.5",  // Backend dependencies
    "express": "^4.17.1",
    "mongoose": "^5.9.10",
    "react": "^17.0.2",  // Frontend dependencies
    "react-dom": "^17.0.2",
    "react-scripts": "^4.0.3"
  }
}
